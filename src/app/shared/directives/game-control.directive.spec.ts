import { GameControlDirective } from './game-control.directive';

describe('GameControlDirective', () => {
  it('should create an instance', () => {
    const directive = new GameControlDirective();
    expect(directive).toBeTruthy();
  });
});
